[package]
name = "{{project-name}}"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]

[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = true
strip = true
codegen-units = 1
opt-level = 3
panic = 'abort'
