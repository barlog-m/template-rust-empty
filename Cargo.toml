[package]
name = "{{project-name}}"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = true
strip = true
codegen-units = 1
panic = 'abort'
